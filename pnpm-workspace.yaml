allowBuilds:
  esbuild: true
  unrs-resolver: true

# savePrefix '' pins exact versions on `pnpm add`, matching this repo's
# convention of exact-pinned dependencies (no ^ or ~ ranges).
savePrefix: ''
